# Magnific Cookie Sync (Internal Chrome / Edge extension)

Legacy fallback only. Normal designer setup uses `MagnificStock.exe` and does not need this extension.

The extension remains for development/debugging and sends cookies to the local proxy only.
